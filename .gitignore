# TLS certs / secrets — never commit
server/certs/
*.pem
*.key
.env

# APK signing material — never commit (keystore lives outside this repo)
*.keystore
*.jks
*.b64
keystore-password*.txt

# build artifacts
*.apk
__pycache__/
*.pyc

# android local config
local.properties
